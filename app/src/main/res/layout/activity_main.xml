<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/loginstory"> <!-- Background image -->

    <!-- Email EditText centered in the parent -->
    <EditText
        android:id="@+id/emailEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:backgroundTint="@color/white"
        android:hint="Email"
        android:padding="10dp"
        android:textColor="@color/black"
        android:layout_centerInParent="true" />

    <!-- Password EditText below the email field -->
    <EditText
        android:id="@+id/passwordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:inputType="textPassword"
        android:layout_margin="16dp"
        android:backgroundTint="@color/white"
        android:padding="10dp"
        android:textColor="@color/black"
        android:layout_below="@id/emailEditText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />

    <!-- LinearLayout for side-by-side buttons -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/passwordEditText"
        android:layout_centerHorizontal="true"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">

        <!-- Login Button (Small size and green color) -->
        <Button
            android:id="@+id/loginButton"
            android:layout_width="90dp"
            android:layout_height="40dp"
            android:text="Login"
            android:layout_marginEnd="10dp"
            android:padding="6dp"
            android:backgroundTint="@color/green"
            android:textColor="@color/white" />

        <!-- Register Button (Small size and green color) -->
        <Button
            android:id="@+id/registerButton"
            android:layout_width="90dp"
            android:layout_height="40dp"
            android:text="Register"

            android:padding="6dp"
            android:backgroundTint="@color/green"
            android:textColor="@color/white" />
    </LinearLayout>
</RelativeLayout>
